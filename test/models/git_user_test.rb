require 'test_helper'

class GitUserTest < ActiveSupport::TestCase

  # test "get_user_repos returns expected JSON and count_language_prefs finds favourite language" do
  #   user = GitUser.new(username: "VincentDesforges")
  #   # user.response = get_user_repos(user.username)
  #   user.favourite = count_language_prefs(get_user_repos(user.username))

  #   assert_equal "Ruby", user.favourite
  # end
end
